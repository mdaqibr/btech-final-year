# Payment Microservice (Razorpay)

## Setup
1. Copy `.env.example` to `.env` and fill required keys:
   - RAZORPAY_KEY_ID
   - RAZORPAY_KEY_SECRET
   - WEBHOOK_SECRET

2. Install:
```bash
npm install
npm start
